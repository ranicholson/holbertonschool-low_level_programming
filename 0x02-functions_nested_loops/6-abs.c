#include "holberton.h"

/**
 * _abs - absolute value function
 *@r: number generated
 * description: function to compute absolute value of a number
 * Return: Hopefully 0!
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
}
