#include "holberton.h"

/**
 * add - adds integers
 * @x: represents a number
 * @y: represents a number
 * Return: return should bbe the vaue of two integers added together
 */

int add(int x, int y)
{
	return (x + y);
}
